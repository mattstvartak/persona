Show behavioral profile. $ARGUMENTS

If arguments include "detailed":
1. Call `persona_stats` for the complete picture
2. Show signal counts by type, proposal history, soul file sizes
3. Include the profile summary too

Otherwise (default):
1. Call `persona_profile` for the readable summary
2. Cover: satisfaction rate, verbosity preference, key style preferences, avoid patterns, preferred patterns, topic preferences
3. Frame it as "here's what I've picked up about how you like to work"

Be genuine about what the system knows and doesn't know. If there's limited data, say so. If satisfaction is low, acknowledge it.
